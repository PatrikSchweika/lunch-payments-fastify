{
	"name": "fastify-test",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"author": "",
	"license": "ISC",
	"scripts": {
		"format": "biome check --write",
		"lint": "biome check",
		"build": "pnpm -r build && node -e \"fs.cpSync('client/dist', 'server/public', { recursive: true })\"",
		"dev": "pnpm -r dev",
		"kill": "kill-port 5173 & kill-port 8080"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"@types/node": "^24.2.0",
		"kill-port": "^2.0.1",
		"typescript": "^5.9.2"
	}
}
